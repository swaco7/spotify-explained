<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <import type="android.view.View" />
        <variable
            name="phase"
            type="Integer"/>
        <variable
            name="targetPhase"
            type="Integer"/>
        <variable
            name="name"
            type="String" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="5dp">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:orientation="horizontal">

        <RelativeLayout
            android:layout_width="0dp"
            android:layout_weight="0.25"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:id="@+id/icon_layout">
            <ImageView
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_centerVertical="true"
                android:layout_alignParentEnd="true"
                android:src="@{phase &lt; targetPhase ? @drawable/check_box_outline_blank_48px : @drawable/check_box_48px}"
                android:visibility="@{((phase > targetPhase) || (phase &lt; targetPhase)) ? View.VISIBLE : View.GONE}">
            </ImageView>
            <ProgressBar
                android:layout_alignParentEnd="true"
                style="?android:attr/progressBarStyle"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:layout_centerVertical="true"
                android:layout_gravity="end"
                android:visibility="@{((phase > targetPhase) || (phase &lt; targetPhase)) ? View.GONE : View.VISIBLE}" />
        </RelativeLayout>
        <TextView
            android:layout_marginStart="10dp"
            android:layout_width="0dp"
            android:layout_weight="0.75"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:text="@{name}">
        </TextView>
        </LinearLayout>
    </RelativeLayout>
</layout>